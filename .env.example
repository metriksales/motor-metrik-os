# Motor Metrik OS — variáveis de ambiente (NUNCA commitar valores reais)

# Banco (Neon Postgres, pooled)
DATABASE_URL=postgres://user:pass@ep-xxx.neon.tech/db?sslmode=require

# Control API — auth temporária (service token). Clerk substitui.
CONTROL_PLANE_SECRET=troque-isto-por-um-segredo-forte

# Auth (Clerk) — próximo slice
CLERK_SECRET_KEY=
VITE_CLERK_PUBLISHABLE_KEY=

# Redis / filas (Upstash) — quando ligar o runtime
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
